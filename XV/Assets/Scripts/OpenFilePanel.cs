using System.IO;
using UnityEngine;
using UnityEditor;

public class OpenFilePanel : EditorWindow
{
    public void OpenFile()
    {
        // OnClick import model - show UI
        // UI is box with vertical layout, and content size fitter
        // Add text element for each file found

        //string[] lFiles = Directory.GetFiles(Application.dataPath + "/Resources/SavedData/Models");
        //foreach (string lFile in lFiles)
        //{
        //    // 
        //}
    }
}